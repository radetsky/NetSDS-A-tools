context mobiles { 
	_050X. => { 
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT); 
		Hangup(); 
	}
	_066X. => {
                &out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
        }
	_095X. => {
                &out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
        }
	_099X. => {
                &out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
        }
	_067X. => {
                &out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
        }
	_068X. => { 
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
	}
	_096X. => {
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
	}
	_097X. => {
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
	}
	_098X. => {
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
	}
	_063X. => { 
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
	} 
	_093X. => { 
		&out-call(${MY_CALLERID});
		Dial(SIP/voip-gate/${EXTEN},120,tT);
                Hangup();
	}
};

